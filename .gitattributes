# Normalise to LF in the repository. Windows checkouts still get CRLF where the
# platform wants it, and the Linux CI runner sees consistent files either way.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
